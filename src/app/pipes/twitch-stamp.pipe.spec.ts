import { TwitchStampPipe } from './twitch-stamp.pipe';

describe('TwitchStampPipe', () => {
  it('create an instance', () => {
    const pipe = new TwitchStampPipe();
    expect(pipe).toBeTruthy();
  });
});
