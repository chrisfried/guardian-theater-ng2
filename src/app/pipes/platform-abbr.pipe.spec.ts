import { PlatformAbbrPipe } from './platform-abbr.pipe';

describe('PlatformAbbrPipe', () => {
  it('create an instance', () => {
    const pipe = new PlatformAbbrPipe();
    expect(pipe).toBeTruthy();
  });
});
